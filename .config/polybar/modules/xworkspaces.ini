[module/xworkspaces]
background = ${colors.bg}
type = internal/xworkspaces
label-active = %name%
label-active-foreground = ${colors.bg}
label-active-background = ${colors.green_h}
label-active-underline= ${colors.green_h}
label-active-padding = 1 

label-occupied = %name%
label-occupied-padding = 1

label-urgent = %name%
label-urgent-background = ${colors.red_h}
label-urgent-padding = 1

label-empty = %name%
label-empty-foreground = ${colors.aqua_s}
label-empty-padding = 1
